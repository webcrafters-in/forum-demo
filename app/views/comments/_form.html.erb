<%= form_for([@forem, @forem.comments.build]) do |comment| %>
    <%= comment.label "Add Comments" %>
    <%= comment.text_area :comment, :class => "redactor" %>
    <%= comment.submit "Post Comment", :class => "btn btn-default" %>
<% end %>